# v0.8.2: 分割レビュー品質改善（3件）

作成日: 2026-02-24

## 背景

分割レビュー機能を実際の設計書・プログラムで利用した際に、以下の3つの問題が確認された。

1. 設計書を含まないグループが作成されてしまう
2. 全グループで重要なシートを参照したい
3. メソッドが未実装と指摘されてしまう

いずれも分割レビューのグルーピング・グループレビューの品質に関わる問題であり、プロンプト改善・UI追加・API拡張で対応する。

## ゴール

- 構造マッチングで、設計書セクションとコードシンボルの**両方が必ず1件以上含まれるグループ**のみ生成される
- 分割プレビュー時に**重要パートを選択**し、全グループのレビューで共通参照される
- グループレビュー時に**設計書・コード全体の構造情報（INDEX.md / MAP.json）がAIに渡される**ことで、他グループに実装されている関数を「未実装」と誤指摘しなくなる

## 非ゴール

- 構造マッチングのアルゴリズム自体の変更（LLMベースのまま）
- 対応言語の追加（Python / Java のまま）
- 分割プレビューUIの大幅なデザイン変更

---

## 改善1: 設計書を含まないグループが作成されてしまう

### 問題の詳細

構造マッチング（フェーズ1）のプロンプトに、「各グループには設計書セクションとコードシンボルの両方を含めること」という制約がないため、LLMがコードだけ、または設計書だけのグループを生成してしまう。

### 実装方針

プロンプトへの制約追加で対応する。

### Step 1-a. 構造マッチングプロンプトに制約を追加

対象: `versions/v0.8.2/backend/app/routers/review.py:246-253`（構造マッチングのnotes_parts）

現在:
```python
notes_parts = [
    "- 必ず指定されたJSON形式のみで応答してください",
    "- 設計書の複数セクションと、複数のコード部分が、1つのグループに対応する場合もあります。",
    "- 同じ設計書セクション、コード部分が、複数のグループに対応する場合もあります。",
    "- 文字数の少ないセクション、コードシンボルは、情報が含まれていない可能性があります。他の部分と合わせてグループ化を検討してください。",
    "- 【重要】出力するdoc_sectionsのidは、設計書MAP.jsonに記載されたid値を正確にそのまま使用してください（例: MD1, MD2, ...）",
    "- 【重要】出力するcode_symbolsのidは、コードMAP.jsonに記載されたid値を正確にそのまま使用してください（例: CD1, CD2, ...）",
]
```

変更後（2行追記）:
```python
notes_parts = [
    "- 必ず指定されたJSON形式のみで応答してください",
    "- 【重要】各グループには、doc_sections（設計書セクション）とcode_symbols（コードシンボル）の両方を必ず1件以上含めてください。設計書だけ、またはコードだけのグループは作成しないでください。",
    "- 【重要】すべての設計書セクションとコードシンボルが、いずれかのグループに含まれるようにしてください。どのグループにも含まれない設計書セクションやコードシンボルがないようにしてください。",
    "- 設計書の複数セクションと、複数のコード部分が、1つのグループに対応する場合もあります。",
    "- 同じ設計書セクション、コード部分が、複数のグループに対応する場合もあります。",
    "- 文字数の少ないセクション、コードシンボルは、情報が含まれていない可能性があります。他の部分と合わせてグループ化を検討してください。",
    "- 【重要】出力するdoc_sectionsのidは、設計書MAP.jsonに記載されたid値を正確にそのまま使用してください（例: MD1, MD2, ...）",
    "- 【重要】出力するcode_symbolsのidは、コードMAP.jsonに記載されたid値を正確にそのまま使用してください（例: CD1, CD2, ...）",
]
```

### Step 1-b. spec.md の構造マッチングAPI仕様を更新

対象: `versions/v0.8.2/spec.md`（構造マッチングAPIの説明セクション）

制約条件の記載を追加:
- 各グループには `doc_sections` と `code_symbols` の両方が1件以上必要

---

## 改善2: 全グループで重要なシートを参照したい

### 問題の詳細

設計書が複数シートで構成されている場合（例: 1シート目がチェック条件表、2シート目以降が別紙）、構造マッチングで別紙ごとにグループが作成される。しかし、チェック条件表のような重要シートは全グループで参照する必要がある。

### 実装方針

分割プレビュー結果の設計書パーツテーブルに「全グループ共通」チェックボックスを追加し、選択されたパーツは構造マッチング結果に関わらず全グループのレビューに含める。

### Step 2-a. 型定義にピン留めパーツIDを追加

対象: `versions/v0.8.2/frontend/src/features/reviewer/types/index.ts:226-234`（SplitPreviewResult）

変更後:
```typescript
export interface SplitPreviewResult {
  documentParts: DocumentPart[] | null
  codeParts: CodePart[] | null
  documentIndex: string | null
  documentMapJson: Record<string, unknown>[] | null
  codeIndex: string | null
  codeMapJson: Record<string, unknown>[] | null
  codeLanguage: string | null
  pinnedDocPartIds: string[]  // 追加: 全グループ共通の設計書パーツID
}
```

### Step 2-b. useSplitSettings にピン留め状態管理を追加

対象: `versions/v0.8.2/frontend/src/features/reviewer/hooks/useSplitSettings.ts`

追加する状態と関数:
```typescript
const [pinnedDocPartIds, setPinnedDocPartIds] = useState<string[]>([])

const togglePinnedDocPart = useCallback((partId: string) => {
  setPinnedDocPartIds(prev =>
    prev.includes(partId)
      ? prev.filter(id => id !== partId)
      : [...prev, partId]
  )
}, [])
```

- `UseSplitSettingsReturn` に `pinnedDocPartIds` と `togglePinnedDocPart` を追加
- `clearPreview` 実行時に `pinnedDocPartIds` もクリア
- `setPreviewResult` 時に `pinnedDocPartIds` を含める

### Step 2-c. 分割プレビューUIにチェックボックスを追加

対象: `versions/v0.8.2/frontend/src/features/reviewer/components/SplitSettingsSection.tsx:322-347`（DocumentPartsTable）

設計書パーツテーブルに「全グループ共通」チェックボックス列を追加する。

画面イメージ:
```
┌─ プレビュー結果 ─────────────────────────────────────────────────────┐
│                                                                      │
│  ■ 設計書: 5 パート                                                   │
│  **重要**にチェックしたセクションは、分割レビュー時に全てのグループで   │
│  参照されます。                                                       │
│                                                                      │
│  ┌──────┬────┬──────────────────────┬──────────┬────────────┐        │
│  │ 重要  │ #  │ セクション名          │ 行範囲    │ 推定トークン │        │
│  ├──────┼────┼──────────────────────┼──────────┼────────────┤        │
│  │ [✓]  │ 1  │ チェック条件表         │ L1-L120  │ ~4,500     │        │
│  │ [ ]  │ 2  │ 別紙1: 入力仕様       │ L121-L200│ ~2,800     │        │
│  │ [ ]  │ 3  │ 別紙2: 出力仕様       │ L201-L280│ ~2,200     │        │
│  │ [ ]  │ 4  │ 別紙3: エラー処理     │ L281-L350│ ~1,900     │        │
│  │ [✓]  │ 5  │ 別紙4: テーブル定義   │ L351-L400│ ~1,500     │        │
│  └──────┴────┴──────────────────────┴──────────┴────────────┘        │
│                                                                      │
│  ■ プログラム: 8 パート (python)                                      │
│                                                                      │
│  ┌────┬──────────────────────┬──────────┬──────────┬────────────┐     │
│  │ #  │ シンボル名            │ 種別      │ 行範囲   │ 推定トークン│     │
│  ├────┼──────────────────────┼──────────┼──────────┼────────────┤     │
│  │ 1  │ InputValidator       │ class    │ L10-L80  │ ~2,100     │     │
│  │ 2  │ InputValidator#check │ method   │ L25-L60  │ ~1,200     │     │
│  │ ...│ ...                  │ ...      │ ...      │ ...        │     │
│  └────┴──────────────────────┴──────────┴──────────┴────────────┘     │
└──────────────────────────────────────────────────────────────────────┘
```

`SplitSettingsSectionProps` に追加:
```typescript
pinnedDocPartIds: string[]
onTogglePinnedDocPart: (partId: string) => void
```

`DocumentPartsTable` に追加:
```typescript
function DocumentPartsTable({
  parts,
  pinnedDocPartIds,
  onTogglePinnedDocPart,
}: {
  parts: DocumentPart[]
  pinnedDocPartIds: string[]
  onTogglePinnedDocPart: (partId: string) => void
}) {
  // テーブルヘッダーに「全グループ共通」列追加
  // 各行にチェックボックス追加
}
```

### Step 2-d. 構造マッチング結果に重要パートを注入

対象: `versions/v0.8.2/frontend/src/features/reviewer/index.tsx:332-346`（Phase 1 完了後、Phase 2 開始前）

Phase 1 の構造マッチング結果を受け取った直後に、重要パートを全グループの `docSections` に注入する。
これにより、既存の documentContent 構築ロジック・実行画面の表示・統合フェーズのすべてが変更なしで正しく動作する。

現在:
```typescript
const groups = structureMatchingResponse.groups

// Initialize group review states
const initialGroupStates: GroupReviewState[] = groups.map((g) => ({
```

変更後:
```typescript
const groups = structureMatchingResponse.groups

// 重要パートを全グループに注入（重複除外）
const pinnedDocPartIds = splitPreviewResult.pinnedDocPartIds || []
if (pinnedDocPartIds.length > 0) {
  const pinnedDocSections = pinnedDocPartIds
    .map(id => {
      const part = splitPreviewResult.documentParts?.find(p => p.id === id)
      if (!part) return null
      return { id: part.id, title: part.section, path: part.path }
    })
    .filter((s): s is MatchedDocSection => s !== null)

  for (const group of groups) {
    const existingIds = new Set(group.docSections.map(s => s.id))
    for (const pinned of pinnedDocSections) {
      if (!existingIds.has(pinned.id)) {
        group.docSections.push(pinned)
      }
    }
  }
}

// Initialize group review states
const initialGroupStates: GroupReviewState[] = groups.map((g) => ({
```

### Step 2-e. spec.md の分割レビュー仕様を更新

対象: `versions/v0.8.2/spec.md`（分割レビューの説明セクション）

- 分割プレビュー結果で「重要」パートを選択できる旨を追記
- 重要パートは構造マッチング後に全グループの `docSections` に注入される旨を追記
- `SplitPreviewResult` に `pinnedDocPartIds` フィールドが追加された旨を追記

---

## 改善3: メソッドが未実装と指摘されてしまう

### 問題の詳細

グループレビュー（フェーズ2）では、そのグループに割り当てられた設計書セクションとコードシンボルのみがAIに渡される。そのため、他のグループに含まれるメソッドや関数が存在しないものと判断され、「未実装」と誤指摘されてしまう。

### 実装方針

グループレビューAPIのリクエストに全体の構造情報（INDEX.md / MAP.json）を追加し、プロンプトでも「全体の一部をレビューしている」ことを強調する。

### Step 3-a. GroupReviewRequest にコンテキストフィールドを追加

対象: `versions/v0.8.2/backend/app/models/schemas.py:435-448`（GroupReviewRequest）

現在:
```python
class GroupReviewRequest(BaseModel):
    groupId: str
    groupName: str
    documentContent: str
    codeContent: str
    reviewOptions: dict = {}
    systemPrompt: SystemPrompt | None = None
    llmConfig: LLMConfig | None = None
```

変更後:
```python
class GroupReviewRequest(BaseModel):
    groupId: str
    groupName: str
    documentContent: str
    codeContent: str
    reviewOptions: dict = {}
    systemPrompt: SystemPrompt | None = None
    llmConfig: LLMConfig | None = None
    # 全体構造コンテキスト（他グループの存在を把握するため）
    documentIndexMd: str | None = None    # 設計書全体のINDEX.md
    documentMapJson: dict | None = None   # 設計書全体のMAP.json
    codeIndexMd: str | None = None        # コード全体のINDEX.md
    codeMapJson: list[dict] | None = None # コード全体のMAP.json
    allGroups: list[dict] | None = None   # 構造マッチング結果の全グループ情報
```

### Step 3-b. TypeScript型定義を更新

対象: `versions/v0.8.2/frontend/src/features/reviewer/types/index.ts:291-299`（GroupReviewRequest）

変更後:
```typescript
export interface GroupReviewRequest {
  groupId: string
  groupName: string
  documentContent: string
  codeContent: string
  reviewOptions?: Record<string, unknown>
  systemPrompt?: SystemPromptValues
  llmConfig?: LlmConfig
  // 全体構造コンテキスト
  documentIndexMd?: string
  documentMapJson?: Record<string, unknown>
  codeIndexMd?: string
  codeMapJson?: Record<string, unknown>[]
  allGroups?: MatchedGroup[]
}
```

### Step 3-c. グループレビュープロンプトに全体構造コンテキストを追加

対象: `versions/v0.8.2/backend/app/routers/review.py:391-404`（グループレビューのnotes_parts）

現在:
```python
notes_parts = [
    "- 提供されている設計書・コードは元ファイルの一部分であり、完全な情報が含まれていない可能性があります",
    "- 最後に複数グループのレビュー結果を統合するので、統合時への申し送り事項があれば記載してください",
    "- 元ファイルでの行番号範囲はヘッダーコメント（`// lines: X-Y`）に記載されているので、行番号を参照する際はこの範囲に基づいて記載してください",
]
```

変更後:
```python
notes_parts = [
    "- 【重要】提供されている設計書・コードは元ファイルの一部分です。全体構造情報（INDEX.md / MAP.json）で示される他の部分は別のグループでレビューされています。",
    "- 【重要】別のグループでレビューされている関数・メソッド・クラスについて「未実装」「存在しない」と指摘しないでください。全体構造情報を参照し、そのシンボルが他のグループに存在する場合は、実装済みと判断してください。",
    "- 最後に複数グループのレビュー結果を統合するので、統合時への申し送り事項があれば記載してください",
    "- 元ファイルでの行番号範囲はヘッダーコメント（`// lines: X-Y`）に記載されているので、行番号を参照する際はこの範囲に基づいて記載してください",
]
```

### Step 3-d. グループレビューのユーザーメッセージに全体構造情報を追加

対象: `versions/v0.8.2/backend/app/routers/review.py:408-419`（グループレビューのユーザーメッセージ構築）

現在:
```python
user_parts = [
    f"## レビュー対象グループ: {request.groupName}\n",
    f"- グループID: {request.groupId}\n",
    "## 設計書内容\n",
    request.documentContent,
    "\n## コード内容\n",
    request.codeContent,
]
```

変更後（レビュー対象を先に、全体構造情報を参考として末尾に配置）:
```python
user_parts = [
    f"## レビュー対象グループ: {request.groupName}\n",
    f"- グループID: {request.groupId}\n",
    "## 設計書内容\n",
    request.documentContent,
    "\n## コード内容\n",
    request.codeContent,
]

# 全体構造コンテキスト（他グループの存在を把握するため、参考情報として末尾に配置）
if request.documentIndexMd or request.codeIndexMd or request.allGroups:
    user_parts.append("\n## 全体構造情報（参考）\n")
    user_parts.append("以下は設計書・コード全体の構造です。このグループではこの一部をレビューしています。他のグループに含まれるシンボルについて「未実装」と指摘しないでください。\n")
    # INDEX.md, MAP.json, 全グループ一覧を追加（省略）
```

### Step 3-e. フロントエンドからグループレビュー呼び出し時にコンテキストを渡す

対象: `versions/v0.8.2/frontend/src/features/reviewer/index.tsx:392-398`（executeGroupReview呼び出し）

現在:
```typescript
const groupResponse = await executeGroupReview({
  groupId: group.groupId,
  groupName: group.groupName,
  documentContent,
  codeContent,
  systemPrompt: currentPromptValues,
  llmConfig: llmConfig || undefined,
})
```

変更後:
```typescript
const groupResponse = await executeGroupReview({
  groupId: group.groupId,
  groupName: group.groupName,
  documentContent,
  codeContent,
  systemPrompt: currentPromptValues,
  llmConfig: llmConfig || undefined,
  documentIndexMd: splitPreviewResult.documentIndex || undefined,
  documentMapJson: splitPreviewResult.documentMapJson
    ? { sections: splitPreviewResult.documentMapJson }
    : undefined,
  codeIndexMd: splitPreviewResult.codeIndex || undefined,
  codeMapJson: splitPreviewResult.codeMapJson || undefined,
  allGroups: structureMatchingResponse.groups,
})
```

### Step 3-f. spec.md のグループレビューAPI仕様を更新

対象: `versions/v0.8.2/spec.md`（グループレビューAPIの説明セクション）

- リクエストパラメータに `documentIndexMd`, `documentMapJson`, `codeIndexMd`, `codeMapJson`, `allGroups` を追記
- 「全体の一部をレビューしている」旨の説明を追記

---

## 影響ファイル一覧

| ファイル | 改善 | 変更内容 |
|---------|------|---------|
| `versions/v0.8.2/backend/app/routers/review.py` | 1, 3 | 構造マッチングプロンプト制約追加、グループレビュープロンプト・メッセージ改善 |
| `versions/v0.8.2/backend/app/models/schemas.py` | 3 | GroupReviewRequest にコンテキストフィールド追加 |
| `versions/v0.8.2/frontend/src/features/reviewer/types/index.ts` | 2, 3 | SplitPreviewResult にピン留め、GroupReviewRequest にコンテキスト追加 |
| `versions/v0.8.2/frontend/src/features/reviewer/hooks/useSplitSettings.ts` | 2 | ピン留め状態管理追加 |
| `versions/v0.8.2/frontend/src/features/reviewer/components/SplitSettingsSection.tsx` | 2 | 設計書パーツテーブルにチェックボックス列追加 |
| `versions/v0.8.2/frontend/src/features/reviewer/index.tsx` | 2, 3 | ピン留めパーツ含有ロジック、コンテキスト渡し |
| `versions/v0.8.2/spec.md` | 1, 2, 3 | 構造マッチング・重要パート・グループレビューAPI仕様の更新 |

---

## 完了チェック表

| ステップ | 内容 | 状態 |
|---------|------|------|
| **改善1: 設計書を含まないグループ** | | |
| Step 1-a | 構造マッチングプロンプトに制約追加 | ○ |
| Step 1-b | spec.md の仕様更新 | ○ |
| **改善2: 全グループで重要シート参照** | | |
| Step 2-a | 型定義にピン留めパーツID追加 | ○ |
| Step 2-b | useSplitSettings にピン留め状態管理追加 | ○ |
| Step 2-c | プレビューUIにチェックボックス追加 | ○ |
| Step 2-d | 構造マッチング結果に重要パートを注入 | ○ |
| Step 2-e | spec.md の仕様更新 | ○ |
| **改善3: メソッド未実装の誤指摘** | | |
| Step 3-a | GroupReviewRequest にコンテキストフィールド追加（Python） | ○ |
| Step 3-b | GroupReviewRequest にコンテキストフィールド追加（TypeScript） | ○ |
| Step 3-c | グループレビュープロンプト改善 | ○ |
| Step 3-d | ユーザーメッセージに全体構造情報追加 | ○ |
| Step 3-e | フロントエンドからコンテキスト渡し | ○ |
| Step 3-f | spec.md の仕様更新 | ○ |

---

## 試験項目表

> 手動動作確認チェックリスト

### 改善1: 設計書を含まないグループが作成されてしまう

- [ ] 分割レビューを実行し、構造マッチング結果の全グループに設計書セクションとコードシンボルが両方含まれていること
- [ ] 設計書セクション数 > コードシンボル数のケースで、設計書のみのグループが作成されないこと
- [ ] コードシンボル数 > 設計書セクション数のケースで、コードのみのグループが作成されないこと

### 改善2: 全グループで重要なシートを参照したい

- [ ] 分割プレビュー結果の設計書パーツテーブルに「全グループ共通」チェックボックスが表示されること
- [ ] チェックを付けたパーツが、すべてのグループレビューの設計書内容に含まれること
- [ ] チェックを付けたパーツが、そのグループに元々割り当てられている場合は重複して含まれないこと
- [ ] チェックを付けていない状態では、従来通りの動作であること
- [ ] 分割プレビューをクリアするとチェック状態もリセットされること

### 改善3: メソッドが未実装と指摘されてしまう

- [ ] グループレビュー結果に、他グループに実装されている関数について「未実装」と指摘されないこと
- [ ] 設計書・コード全体の構造情報（INDEX.md / MAP.json）がグループレビューのコンテキストとして渡されていること（APIリクエストで確認）
- [ ] 全グループ一覧テーブルがユーザーメッセージに含まれていること
- [ ] 全体構造コンテキストが追加されても、レビュー品質が低下しないこと（トークン上限に余裕があること）

### 回帰テスト

- [ ] 一括レビュー（batch）モードが従来通り動作すること
- [ ] 分割レビューのフェーズ1→フェーズ2→フェーズ3の全体フローが正常に完了すること
- [ ] グループレビューのエラー時にリトライ・スキップが正常に動作すること
- [ ] 結果統合のレポートに分割レビューのメタ情報（グループ分け結果テーブル）が含まれること
