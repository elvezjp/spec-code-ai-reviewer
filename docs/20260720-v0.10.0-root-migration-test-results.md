# 試験項目表：versions/ 廃止後のルート構成での起動・動作確認（v0.10.0）

実施日: 2026-07-20
対象ブランチ: `tominaga/20260720-versions-dir-removal-plan`（PR [#122](https://github.com/elvezjp/spec-code-ai-reviewer/pull/122)）
関連計画書: [20260720-versions-dir-removal-v0.10.0-plan.md](20260720-versions-dir-removal-v0.10.0-plan.md)

**起動環境**:
- バックエンド: `cd backend && uv run uvicorn app.main:app --port 8000`
- フロントエンド: `cd frontend && npm run dev`（http://localhost:5173）

**結果欄の凡例**: ✅ = 確認済み（問題なし）

全 25 項目確認済み。N-015〜N-017・E-006・M-002 は手動操作（画面操作・LLM 実行・実機）で確認した（2026-07-20）。それ以外は自動確認。

## 1. 正常系

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| N-001 | バックエンドがルート構成で起動できること | `backend/` で `uv sync` 済み | 1. `cd backend`<br>2. `uv run uvicorn app.main:app --port 8000` | エラーなく起動する | ✅ |
| N-002 | フロントエンドがルート構成で起動できること | `frontend/` で `npm install` 済み | 1. `cd frontend`<br>2. `npm run dev` | Vite が 5173 で起動する | ✅ |
| N-003 | ヘルスチェックが新バージョンを返すこと | 両サーバー起動済み | 1. `GET /api/health` を実行 | `{"status":"healthy","version":"0.10.0"}` が返る | ✅ |
| N-004 | メイン画面が表示され、バージョン切替バルーンが表示されないこと | 両サーバー起動済み | 1. http://localhost:5173 を開く | 画面が表示され、ヘッダー左上に旧ピル型バージョンボタンがない | ✅ |
| N-005 | 設定モーダルに起動中バージョンが表示されること | メイン画面表示中 | 1. 右上の歯車アイコンをクリック | 「バージョン: v0.10.0」が表示され、FE/BE バージョン不一致警告が出ない | ✅ |
| N-006 | 変換ツール一覧が取得できること | バックエンド起動済み | 1. `GET /api/convert/available-tools` | MarkItDown / excel2md (CSV) / excel2md (CSV+Mermaid) の3件が返る | ✅ |
| N-007 | excel2md 変換が PyPI 版で成功すること | テスト用 xlsx あり | 1. `POST /api/convert/excel-to-markdown`（tool=excel2md） | CSVマークダウンが返る（出力ヘッダに PyPI 版の仕様バージョン 2.3.0 を確認） | ✅ |
| N-008 | excel2md (CSV+Mermaid) 変換が成功すること | 同上 | 1. 同 API（tool=excel2md_mermaid） | CSVマークダウンが返る | ✅ |
| N-009 | MarkItDown 変換が成功すること | 同上 | 1. 同 API（tool=markitdown） | シート内容のテーブルが返る | ✅ |
| N-010 | add-line-numbers 変換が成功すること | テスト用 Java ファイルあり | 1. `POST /api/convert/add-line-numbers` | 行番号付きテキストが返る（5行） | ✅ |
| N-011 | md2map による見出し抽出が成功すること | バックエンド起動済み | 1. `POST /api/split/headings` に Markdown を送信 | H1/H2 見出しが構造化されて返る | ✅ |
| N-012 | code2map によるコード分割が成功すること | 同上 | 1. `POST /api/split/code` に Java コードを送信 | クラス単位の parts / INDEX / MAP が返る | ✅ |
| N-013 | 設定ファイルジェネレーター画面が表示されること | フロント起動済み | 1. /config-file-generator を開く | 画面が表示され、info の version が「v0.10.0（固定）」 | ✅ |
| N-014 | プリセットライブラリ画面が表示されること | フロント起動済み | 1. /presets を開く | プリセット一覧・フィルタが表示される | ✅ |
| N-015 | 画面操作でファイル変換〜プレビュー表示ができること | ブラウザで画面を開いている | 1. 設計書に Excel を選択し「マークダウンに変換」<br>2. プログラムにコードを選択し「add-line-numbersで変換」 | 双方のプレビューが表示される | ✅ |
| N-016 | 一括レビューが実行できること | LLM 認証情報（Bedrock 環境変数または設定ファイル） | 1. 設計書・コードを変換後「レビュー実行」 | レビューレポートが2回分表示される | ✅ |
| N-017 | 分割レビューが実行できること | 同上 + 大きめの入力 | 1. 分割モードで構造マッチング→グループレビュー→統合 | 統合レポートが表示される | ✅ |

## 2. 異常系

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| E-001 | `app_version` Cookie が新規に書き込まれないこと | 初回アクセス | 1. メイン画面を開く<br>2. `document.cookie` を確認 | Cookie が空（切替機能の残骸がない） | ✅ |
| E-002 | 旧 `app_version` Cookie が残っていても無視されること | Cookie に `app_version=v0.9.8` を手動設定 | 1. Cookie 設定後ページをリロード | v0.10.0 の画面が正常表示され、エラーが出ない | ✅ |
| E-003 | 非対応ファイルの Excel 変換でエラーメッセージが返ること | テキストファイルあり | 1. .txt を `POST /api/convert/excel-to-markdown` | 「対応していないファイル形式です。…」が返り、サーバーエラーにならない | ✅ |
| E-004 | フロントエンドのコンソールにエラーが出ていないこと | 各画面を遷移済み | 1. メイン／プリセット／ジェネレーター画面を開いてコンソール確認 | エラーが 0 件 | ✅ |
| E-005 | バックエンドログにエラーが出ていないこと | 一連の API 実行後 | 1. uvicorn のログを確認 | エラーログが 0 件 | ✅ |
| E-006 | LLM 認証情報なしでレビュー実行した場合にエラー表示されること | AWS 認証情報・設定ファイルなし | 1. 変換後「レビュー実行」を押下 | 画面上にエラーメッセージが表示され、アプリがクラッシュしない | ✅ |

## 3. モバイル

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| M-001 | モバイル幅でレイアウトが崩れないこと | ビューポート 375×812 | 1. メイン画面をモバイル幅で表示 | 横スクロールが発生せず、カード・ボタンが画面内に収まる（`scrollWidth == clientWidth == 375` を確認） | ✅ |
| M-002 | 実機のタッチ操作でファイル選択〜変換ができること | iPhone / Android 実機 | 1. 実機ブラウザで開きファイル選択・変換をタップ操作 | 操作が問題なく完了する | ✅ |

## まとめ

- 全 25 項目がすべて問題なし（バックエンドテスト 191 件・フロントエンドテスト 212 件のパスとは別に実施）。
- 自動確認 20 項目に加え、手動確認が必要だった 5 項目（N-015〜N-017、E-006、M-002：画面操作での変換、一括・分割レビュー実行、認証情報なしのエラー表示、実機タッチ操作）も 2026-07-20 に確認完了。
- 今回の変更の核心である以下をエンドツーエンドで確認済み。
  - バージョン切替 UI の消滅と Cookie 非依存（E-001 / E-002）
  - excel2md の PyPI 版動作（N-007 / N-008、出力に仕様バージョン 2.3.0）
  - uv git ソース経由の add-line-numbers / md2map / code2map の動作（N-010〜N-012）
- 補足: ページタイトルが「frontend」なのは v0.9.9 以前からの既存仕様であり、今回の変更起因ではない。
