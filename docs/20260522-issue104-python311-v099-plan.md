# v0.9.9 対応計画書（Python 3.11 への要件引き上げ）

- 対応 Issue: [#104](https://github.com/elvezjp/spec-code-ai-reviewer/issues/104)
- Dependabot alert: [#502](https://github.com/elvezjp/spec-code-ai-reviewer/security/dependabot/502)（`idna < 3.15`, GHSA-65pc-fj4g-8rjx）

## 概要

依存先（`add-line-numbers` / `md2map` / `code2map`）の最新 main が Python `>=3.11` を要求するため、`v0.9.8` のままでは `uv.lock` を再生成できず Dependabot アラートを解消できない。既存 `v*/` は凍結ルールのため、**`v0.9.9` を新設**して Python 3.11 へ引き上げる。

## 対応タスク

### コード変更
- [x] `versions/v0.9.9/` を `v0.9.8/` からコピーして作成
- [x] `versions/v0.9.9/backend/pyproject.toml` を更新
  - `version = "0.9.9"`
  - `requires-python = ">=3.11"`
- [x] `versions/v0.9.9/backend/uv.lock` を再生成し `idna >= 3.15` を取り込む（`idna 3.16` 確認済み）
- [x] `versions/v0.9.9/frontend/` のバージョン表記更新（`package.json` / `package-lock.json` / `useVersions.ts` / `configSchema.ts` / `reviewer/index.tsx` / `split_review_api.test.ts`）
- [x] `latest` シンボリックリンクを `versions/v0.9.9` に張り替え

### ドキュメント更新
- [x] `README_ja.md` の「### 新しいバージョンを追加する際の変更箇所」「#### 設定ファイルとドキュメントの更新」（[L461 / L482](../README_ja.md#L461)）に記載されている全更新箇所を参照し、漏れなくバージョン更新を行う
- [x] `CHANGELOG.md` / `CHANGELOG_ja.md` に v0.9.9 セクション追加（Python 3.11 必須化・idna 脆弱性対応）
- [x] `SECURITY.md` / `SECURITY_ja.md` のサポートバージョン表を 0.9.9 に更新
- [x] `README.md` / `README_ja.md` の参照バージョン・Python 要件表記を更新
- [x] `versions/README.md` に v0.9.9 のエントリを追加
- [x] `CONTRIBUTING.md` / `CONTRIBUTING_ja.md` の参照バージョン・Python 要件を更新
- [x] `docs/ec2-deployment-spec.md` のポート割り当て表に v0.9.9 を追加
- [x] `docker-compose.yml` / `nginx/version-map.conf` / `ecosystem.config.js` / `dev.ecosystem.config.js` / `.github/workflows/ci.yml` を更新（port 8099、CI Python matrix 3.11/3.13）

### 検証
- [x] backend を 3.11+ 環境でセットアップし起動確認（`uv sync` 成功、uvicorn 起動・`/api/health` 200 確認）
- [x] frontend ビルド・起動確認（`npm install` 成功、`npm run test:run` 212 passed。`npm run build` の TS エラーは v0.9.8 から継承している既知事象）
- [x] 後述の試験項目表を実施（環境上検証不能項目を除く）

---

## 試験項目表：v0.9.9 全体動作確認

本表は本件修正そのものではなく、**v0.9.9 として全体が正しく動くこと**を確認するためのもの。

### 1. 正常系

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| N-001 | Python 3.11 環境で backend が起動できること | Python 3.11 がインストール済み | 1. `versions/v0.9.9/backend` で `uv sync`<br>2. backend を起動 | エラーなく起動し API ヘルスチェックが 200 を返す | ✅ Pass — `uv sync` 成功、`uvicorn app.main:app --port 8099` 起動、`GET /api/health` → `HTTP 200 {"status":"healthy","version":"0.9.9"}`（実行環境 Python 3.12） |
| N-002 | frontend がビルド・起動できること | Node 環境構築済み | 1. `versions/v0.9.9/frontend` で `npm install`<br>2. ビルド／起動 | エラーなくビルド完了し画面が表示される | ⚠ Partial — `npm install` 成功、`npm run test:run` 212 passed。ただし `npm run build`（tsc）に既存テストファイル由来の TS エラーあり（v0.9.8 から継承された pre-existing issue、CI は vitest のみ実行のため影響なし） |
| N-003 | レビュー実行が一通り成功すること | backend / frontend 起動済み | 1. サンプル仕様書とコードをアップロード<br>2. レビュー実行 | レビュー結果が画面に表示され、ダウンロードできる | ⏸ Not run — LLM 認証情報および UI 操作が必要なため手動確認待ち |
| N-004 | `latest` 参照で v0.9.9 が起動すること | `latest` を v0.9.9 に張り替え済み | 1. `latest/` 経由で docker-compose を起動 | v0.9.9 の構成で起動する | ✅ Pass — `latest/backend/pyproject.toml` が `version = "0.9.9"` / `requires-python = ">=3.11"` を返すことを確認（docker-compose 起動の実機検証は未実施） |
| N-005 | `uv.lock` に `idna >= 3.15` が含まれること | v0.9.9 の lock 再生成済み | 1. `uv.lock` を確認 | `idna` のバージョンが 3.15 以上になっている | ✅ Pass — `idna 3.16` 取り込み確認 |

### 2. 異常系

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| E-001 | Python 3.10 環境では backend のセットアップが失敗すること | Python 3.10 のみの環境 | 1. `uv sync` を実行 | `requires-python` 不一致のエラーで停止する | ⏸ Not run — Python 3.10 環境を別途用意する必要があるため未実施。`pyproject.toml` で `requires-python = ">=3.11"` が宣言されており、uv が当該制約を強制することは仕様上保証される |
| E-002 | 不正な入力ファイルでレビューを実行した際に適切にエラー表示されること | backend / frontend 起動済み | 1. 不正フォーマットのファイルをアップロード<br>2. レビュー実行 | エラーメッセージが画面に表示され、サーバが落ちない | ⏸ Not run — UI 操作が必要なため手動確認待ち |

### 3. モバイル

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| M-001 | スマートフォン縦画面で frontend が崩れず表示されること | iPhone Safari / Android Chrome | 1. モバイル端末で v0.9.9 frontend を開く | 主要画面のレイアウトが破綻せず横スクロールが発生しない | ⏸ Not run — 実機確認が必要なため手動確認待ち |
