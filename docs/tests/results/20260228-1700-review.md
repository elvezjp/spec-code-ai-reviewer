# AIレビュー結果

## サマリー

- 結果: **OK: 9 / NG: 3 / 全 12 項目**
- NG項目: C5 入力のサニタイズ、C6 セットアップ手順、P3 環境変数の整合性

## AIレビュー

| # | 観点 | 結果 | 備考 |
|---|------|------|------|
| C1 | ファイル名の安全性 | OK | リポジトリ内全ファイル（node_modules等除外）を走査し、OS禁止文字（`< > : " / \ \| ? *`）を含むファイル名は検出されなかった |
| C2 | パス結合 | OK | `versions/v0.9.0/backend/app/` 内の全Pythonファイルを精査。ファイルシステムのパス結合にはすべて `os.path.join` または `pathlib.Path` の `/` 演算子が使用されている。URLパス（APIエンドポイント）の `"/"` は除外して確認済み |
| C3 | ファイル名の大文字小文字 | OK | リポジトリ内全ファイル（node_modules等除外）について、同一ディレクトリ内でbasename を小文字化した際の重複を検査。該当なし |
| C4 | シークレット | OK | ハードコードされたAPIキー・パスワード・トークン・AWS認証情報なし。テストファイル内のダミー値（`"test-api-key"` 等）のみ。`.env` は `.gitignore` で除外済み。`.env.example` にシークレットなし |
| C5 | 入力のサニタイズ | NG | 後述 |
| C6 | セットアップ手順 | NG | 後述 |
| C7 | バージョン指定 | OK | Python 3.10+: README / pyproject.toml / Dockerfile.dev で一致。Node.js 18+: README / Dockerfile.dev（22 LTS）で一致。TypeScript 5.0+: README / package.json（5.9.3）で一致 |
| P1 | バックエンドのバージョン一致 | OK | `pyproject.toml` の `version = "0.9.0"` と `frontend/package.json` の `"version": "0.9.0"` が一致 |
| P2 | API エンドポイントの整合性 | OK | フロントエンド（`api.ts`）から呼び出される全12エンドポイントが、バックエンドのルーター定義（`main.py` の prefix + 各ルーターのパス）と完全に一致 |
| P3 | 環境変数の整合性 | NG | 後述 |
| P4 | ローカル依存パッケージの参照 | OK | `[tool.uv.sources]` の3パッケージ（`add-line-numbers`, `md2map`, `code2map`）すべて `../../../` で解決されるリポジトリルート直下にディレクトリと `pyproject.toml` が存在する |
| P5 | テストカバレッジ | OK | `services/` 配下の全8サービスに対応するテストファイルが `tests/` に存在する |

---

## NG項目の詳細

### C5: 入力のサニタイズ

**指摘1: `SplitMarkdownRequest.filename` / `SplitCodeRequest.filename` のパストラバーサル未検証**（重要度: 中）

- `versions/v0.9.0/backend/app/routers/split.py:87-88`, `split.py:227-228`
- `request.filename` に `../../etc/passwd` のようなパストラバーサル文字列が含まれていた場合、`os.path.join` が `tmpdir` を無視する可能性がある
- `os.path.basename(request.filename)` の使用、またはファイル名に `..` や `/` が含まれないことの検証を推奨

**指摘2: CORS設定がデフォルトで全オリジン許可**（重要度: 中）

- `versions/v0.9.0/backend/app/main.py:24-25`
- 環境変数 `CORS_ORIGINS` 未設定時にデフォルトが `*`（全オリジン許可）かつ `allow_credentials=True`
- デプロイ時の設定漏れでCSRF等のリスクが生じる

**指摘3: `SummarizeRequest.targetType` のバリデーション不足**（重要度: 低）

- `versions/v0.9.0/backend/app/models/schemas.py:566`
- `targetType` が `str` 型で定義されているが、コメントでは `"design" | "code" | "review_result"` に限定すべきことが示唆されている
- `Literal["design", "code", "review_result"]` への変更を推奨

**指摘4: ファイルアップロードの Content-Type ヘッダ未検証**（重要度: 低）

- `versions/v0.9.0/backend/app/routers/convert.py:30-85`
- 拡張子チェックは実施済みだが `file.content_type` の検証がない
- 後段パーサーで不正ファイルは失敗するため実質的被害は限定的

### C6: セットアップ手順

**指摘1: Docker / Docker Compose が前提条件に未記載**（重要度: 高）

- `README.md`, `README_ja.md`
- Docker Compose起動方式の手順があるにもかかわらず、Docker のインストール要件が前提条件セクションに記載されていない

**指摘2: Node.js バージョン要件の不整合**（重要度: 高）

- ルート README（`README.md`, `README_ja.md`）: 「Node.js 18以上」
- `versions/v0.9.0/frontend/README.md`: 「Node.js 20以上」
- バージョン記載が食い違っている

**指摘3: v0.9.0 frontend README のバージョン番号が誤り**（重要度: 中）

- `versions/v0.9.0/frontend/README.md` のタイトルが「# v0.7.0 フロントエンド」のまま v0.9.0 に更新されていない

**指摘4: uv のインストール手順が不十分**（重要度: 低）

- uv のインストール方法がコメントとURL参照のみで、具体的なコマンド例が示されていない

### P3: 環境変数の整合性

以下5個の環境変数がコード内で参照されているがドキュメント未記載:

| # | 環境変数名 | 参照箇所 | デフォルト値 |
|---|-----------|---------|-------------|
| 1 | `BEDROCK_MODEL_ID` | `versions/v0.9.0/backend/app/services/llm_service.py:29` | `global.anthropic.claude-haiku-4-5-20251001-v1:0` |
| 2 | `BEDROCK_MAX_TOKENS` | `versions/v0.9.0/backend/app/services/llm_service.py:32` | `16384` |
| 3 | `EXCEL2MD_PATH` | `versions/v0.9.0/backend/app/markdown_tools/excel2md_tool.py:19` | 内蔵デフォルトパス |
| 4 | `ORGANIZE_TIMEOUT_SECONDS` | `versions/v0.9.0/backend/app/routers/organize.py:21` | `180` |
| 5 | `ORGANIZE_MAX_RETRIES` | `versions/v0.9.0/backend/app/routers/organize.py:22` | `2` |

いずれもデフォルト値があるため動作に支障はないが、運用パラメータとしてドキュメント（`docs/split-review.md` の環境変数テーブル等）への追記を推奨する。
